#+title: Work Plan
#+DATE: <2026-06-10 Wed 13:54>
#+AUTHOR: Mohsen BANAN
#+OPTIONS: toc:4
#+TAGS: me ai aiAndMe

####+BEGIN: b:ai:file/particulars
* AI Working Context
  Working Directory: ../..
  File: /bisos/git/auth/bxRepos/bisos-pip/aiActivity/AI-WorkPlan.org
  Activity: bisos-pip
  Companion Docs: AI-WORKFLOW.org, AI-Activity.org, AI-DevStatus.org, README.org
###+END:

This Work Plan file drives the AI collaborative development.
Interactions are broken down into "Stages". Each Stage has a set
of TODOs that I work with Claude to accomplish.

Note to a fresh Claude: read this file together with AI-WORKFLOW.org,
AI-Activity.org and AI-DevStatus.org to orient yourself. The =<<Context Confirmation>>=
TODO below is the standard first step, or use =/bx-confirm0= as the automated equivalent.
TODOs tagged =:ai:= are assigned to Claude. After completing a TODO, summarize results
under =----- AI Results: ------= and mark it DONE.

* Stage 0: Confirm AI's Understanding of This Repo's Context

Goal: Confirm AI's understanding of the context and capture that in [[Context Confirmation]] TODO.


* Stage 0: TODOs
** +
----- AI Results: ------
** -

* Stage 2: Fully Implement aiActivity.cs

Goal: A functioning implementation with starting point files for py-dblock and userConfigFileParams


* Stage 2 TODOs
** +
** Accomplished and archived. <2026-09-22 Tue 22:15>
** -

* Stage 3: Documentation and pypi upload

Goal: Update README for pypi, Update Blee-Panels documentation, upload to pypi and test as pipx


* Stage 3 TODOs
** +
** Accomplished and archived. <2026-09-22 Tue 22:15>
** -


* Stage 4: Lifecycle Commands

Goal: Add commands for reversible and permanent removal of AI collaboration files.


* Stage 4 TODOs
** +
** Accomplished and archived. <2026-09-22 Tue 22:15>
** -


* Stage 5: Skills Integration

Goal: Distribute Claude Code skills alongside AI collaboration templates.

Skills are self-contained, filesystem-based packages of task-specific
instructions (each a folder with a =SKILL.md=) that Claude loads on-demand
when the current task calls for them. The skill's description sits in
context by default; the full file is read only when relevant.

Two variants:
- *Anthropic pre-built skills* (e.g. =xlsx=, =docx=, =pptx=, =pdf=) — no
  distribution needed; reference by name.
- *Custom skills* — user/org-defined; these are what we distribute through
  the templates tree.

The natural fit with aiActivity's existing =mother/= + activity-specific
override pattern:
- =mother/_skills/= — skills available to every activity (baseline).
- =<activity>/_skills/= — activity-specific skills (e.g. a =bisos-pip-release=
  skill under =bisos-pip/_skills/= for version bumps and PyPI publishing).

=initiate= would symlink =.claude/skills/= the same way it currently
symlinks =.claude/settings.json= and =.claude/commands=. =deClaudify=,
=aiSuspend=, and =aiResume= all need to handle the new =.claude/skills=
symlink the same way they handle the other =.claude/= entries.


* Stage 5 TODOs
** +
** Accomplished and archived. <2026-09-22 Tue 22:15>
** -


* Stage 6: Subproject Overlays via initiateSub

Goal: A separate command that installs only the subproject-specific state
(=CLAUDE.md=, =AI-Activity.org=, =AI-DevStatus.org=, =AI-WorkPlan.org=) in
a subdirectory, requiring an already-=initiate=-d ancestor. The invariants
(=AI-WORKFLOW.org=, =.claude/=) are inherited from the
ancestor via Claude Code's walk-up.

Motivation: nested projects should not re-install the invariants at every
level. Doing so wastes context (Claude reads the same twice)
and creates ambiguity about which activity applies. A slim overlay keeps
subproject state local without duplicating the base.


* Stage 6 TODOs
** +
** Accomplished and archived. <2026-09-22 Tue 22:15>
** -


* Stage 7: Precedence-based templates auto-detect

Goal: Zero-config setup for a non-BISOS user who does the canonical
=git clone https://github.com/bxexamples/aiActivityTemplates ~/aiActivityTemplates=.
Currently =parDefault= for the =templates= param probes only
=/bisos/apps/defaults/ai-templates=; extend to also probe
=~/aiActivityTemplates=, with user-first precedence.


* Stage 7 TODOs
** +
** Accomplished and archived. <2026-09-22 Tue 22:15>
** -


* Stage 8: Retire AI-AGENTS.org — merge into CLAUDE.md

Goal: Eliminate the =AI-AGENTS.org= indirection layer. Historically
=CLAUDE.md= was a 3-line pointer that =@imports= =AI-AGENTS.org=, which
in turn =@imports= =AI-WORKFLOW.org= / =AI-Activity.org= / =AI-DevStatus.org=
/ =AI-WorkPlan.org=. The =AI-AGENTS.org= layer added no functional value:
it was aspirationally vendor-neutral but content-wise 100% Claude-specific,
and =CLAUDE.md= directly importing the four subordinate files is simpler
and equally functional. Merge and delete.


* Stage 8 TODOs
** +
** Accomplished and archived. <2026-09-22 Tue 22:15>
** -


* Stage 9: pipx outside of bisos testing

Goal: Make sure that aiActivity works fine outside of bisos


* Stage 9 TODOs
** +
** Accomplished and archived. <2026-09-22 Tue 22:15>
** -


* Stage 10: Post-launch refinements --- provenance, --noLink, custom activity

Goal: Address rough edges that emerged when using =bisos.aiActivity=
on a real hierarchical project (site-status-scripts) --- three related
changes bundled together because they came out of the same field-usage
session:

1. Provenance recording in safe-copied files (the =templates=<X>= mistake
   would have been caught faster with visible birth-certificate data)
2. =--noLink= CLI override for symlinked files (needed by =custom= activity
   where =AI-Activity.org= must be per-project, not shared)
3. New =custom/= activity in all three templates trees (BISOS, bxexamples),
   for projects whose conventions don't fit any standard
   activity


* Stage 10 TODOs
** +
** Accomplished and archived. <2026-09-22 Tue 22:15>
** -


* Stage 11: CLAUDE.md safe-copy + refresh command

Goal: Fix a resolution bug discovered in a fresh Claude session against
=site-status-scripts=. =CLAUDE.md= was symlinked into the templates
directory. Claude Code resolves symlinks before reading, so
=@./AI-Activity.org= inside the symlinked =CLAUDE.md= resolved against
=<templatesBase>/mother/=, pulling in the wrong =AI-Activity.org=
instead of the project's per-activity one. Silent misinformation ---
critical to fix.

Two related changes:

1. Make =CLAUDE.md= a safe-copy (like =AI-DevStatus.org= /
   =AI-WorkPlan.org=) so its =@./= imports resolve at the project.
   No provenance line --- =CLAUDE.md= must remain equivalent to a
   symlink in content.
2. Add a =refresh= command so template changes to =CLAUDE.md= can be
   pulled in on demand (safe-copy loses the automatic-propagation
   property of symlinks; =refresh= gives it back explicitly).


* Stage 11 TODOs
** +
** Accomplished and archived. <2026-09-22 Tue 22:15>
** -

* Stage 12: Capture the Concept of AI-Outputs.org 

Goal: Capture the Concept of AI-Outputs.org  from ./AI-Outputs.org

* Stage 12 TODOs
** +
** TODO [#A] <<12.1>> Read through AI-Outputs.org - Focus on "Process Design --- Captured, Not Yet Implemented"
SCHEDULED: <2026-09-22 Tue>
- Discuss with me the ** Open items.
- Update py3/bin CSXUs to Create symlinks to .../mother/AI-Outputs.org when creating fresh ones
- Update py3/bin CSXUs to Add missing symlinks to .../mother/AI-Outputs.org for exisiting setups
- And the rest of Work Needed in =bisos.aiActivity=
----- AI Results: ------

Discussed the Open items from =AI-Outputs.org='s "Process Design" section
(now removed from that file --- content consolidated here) and closed each:

- *Destination*: confirmed as an extra symlink hop, not a new repo.
  =mother/AI-Outputs.org= is itself a symlink into a separate blee-binders
  repo (=blee-binders/evolution/aiActivity-outputs/=, renamed from
  =aiActivity-evolution= --- the original name read as "the evolution of
  the aiActivity tool," when the binder actually holds outputs generated
  *through* aiActivity, not about aiActivity itself). This resolves the
  original concern (don't make =mother/= a high-churn write target)
  without inventing new repo machinery in =bisos.aiActivity= --- the tool
  only ever creates one symlink, identical in shape to =AI-WORKFLOW.org=.
  =bxexamples/aiActivityTemplates= is deliberately NOT given this symlink
  now; it will get a real (non-symlinked) file later, with a note that
  BISOS's symlink approach is optional to mirror.
- *In the import chain or not*: decided NOT imported by =CLAUDE.md=.
  Read via =/bx-ai-outputs= (index mode) or opened directly when relevant.
- *Growth*: not a blocking design problem --- an operating-discipline
  matter (drain the =TODO=/=WORKAROUND= backlog regularly).
- *=/bx-ai-outputs= command*: written at
  =mother/_claude/commands/bx-ai-outputs.md=. No argument = index mode,
  grouped =WORKAROUND= first then =TODO= then =INFO=, read-only. An
  argument = capture mode --- drafts an entry (asks for keyword and
  =:REPO:= if unclear, since the fix-repo is often not the current repo),
  shows it for confirmation, then appends under the matching existing
  top-level section (or a new one). Not the only way to write --- the file
  is an ordinary symlinked org file --- but keeps entries consistent.
- *Dangling symlinks*: accepted as a known, pre-existing risk class
  (same as =AI-WORKFLOW.org='s own repo dependency). No action needed.

=bisos.aiActivity= engine changes (=py3/bin/aiActivity.cs=), all following
the existing =AI-WORKFLOW.org= symlink pattern exactly, and all treating
=AI-Outputs.org= as optional (skip silently if the templates tree doesn't
ship it, e.g. =bxexamples=/=rana-notes= today):

- =initiate=: installs the =AI-Outputs.org= symlink from =mother/=.
- =initiateSub=: unchanged by design --- inherits from the parent, exactly
  like =AI-WORKFLOW.org=, rather than installing its own copy.
- =aiSuspend= / =deClaudify=: remove the symlink.
- =aiResume=: reinstalls it, base mode only.
- =refresh=: extended to *backfill* the symlink when missing --- this is
  the retroactive path for already-=initiate=-d projects that predate this
  feature, reusing =refresh= rather than adding a new command. Verified in
  an isolated directory: =MODE: base= → =SYMLINKED (backfilled)=.
- =py3/tests/verify.sh=: extended with coverage for install / remove /
  reinstall / backfill. Full harness run passes (exit 0). One pre-existing
  quirk surfaced during testing, unrelated to this change: running the
  harness from =py3/tests/= --- nested inside this repo's own self-=
  initiate=-d tree --- causes =aiResume=/=refresh= to always detect "sub"
  mode, because the walk-up finds the *parent* aiActivity project's own
  =AI-WORKFLOW.org= symlink even when the target directory has its own
  genuine base install. Pre-existing (same walk-up code path used for
  =AI-WORKFLOW.org= before this stage), not introduced here, and out of
  scope for this stage --- logged as a Stage 14 candidate.
- Documented the =TODO=/=WORKAROUND=/=INFO= keyword vocabulary and the
  =:REPO:= convention in =AI-WORKFLOW.org= (new "AI-Outputs.org ---
  Cross-Project Findings Ledger" section), plus a new table row and a
  =/bx-ai-outputs= row in the slash-commands table.

Status: implementation complete; user is reviewing the diffs before
anything is committed. Not marked DONE yet pending that review.

** -


* Stage 14: Maintenance and New Feature Ideas

Goal: Based on usage consider new features, and non-urgent bugs.

* Stage 14 TODOs
** +
** TODO Mode-detection walk-up misclassifies a nested base install as "sub"
Found while testing Stage 12.1: =aiResume=/=refresh= detect base-vs-sub by
walking up from the *parent* of the target directory looking for an
ancestor =AI-WORKFLOW.org= symlink under templatesBase. If the target
directory sits inside another already-=initiate=-d project (e.g. this
repo's own =py3/tests/=, self-hosted), the walk-up finds the *ancestor's*
symlink and calls it "sub" even when the target directory has its own
genuine base-mode =AI-WORKFLOW.org=. Affects any project nested inside
another initiated project, not just this repo's =tests/= dir.
** -



# Local Variables:
# eval: (setq-local toc-org-max-depth 4)
# eval: (setq-local org-log-done 'time)
# End:
